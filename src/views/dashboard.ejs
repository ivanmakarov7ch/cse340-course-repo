<%- include('partials/header') %>

<main>
    <h1><%= title %></h1>

    <h2>Dashboard</h2>
    <p>Name: <%= user.name %></p>
    <p>Email: <%= user.email %></p>

    <% if (isLoggedIn && user.role_name === 'admin') { %>
        <p><a href="/users">Manage Users</a></p>
    <% } %>
</main>

<%- include('partials/footer') %>